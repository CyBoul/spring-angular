# Docker Compose file is in parent dir; point Spring to it
spring.docker.compose.file=../compose.yml

# if local postgres / spring-boot-docker-support = auto-detect
#spring.datasource.url=jdbc:postgresql://localhost:5432/theotherdatasource
#spring.datasource.name=theotherdatasource
#spring.datasource.username=admin
#spring.datasource.password=password

# to process schema.sql at start (which is auto with embedded database like h2)
spring.sql.init.mode=always

# to drop & recreate JPA DB (which is auto with embedded database like h2)
spring.jpa.hibernate.ddl-auto=create-drop

# ?
#spring.jpa.properties.hibernate.globally_quoted_identifiers=true


